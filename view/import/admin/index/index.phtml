<?php
/**
 * @var \Zend\View\Renderer\PhpRenderer $this
 * @var \Import\Api\Representation\ImporterRepresentation[] $importers
 * @var \Import\Api\Representation\ImportRepresentation[] $imports
 */

use Import\Interfaces\Configurable as ConfigurableImport;

$translate = $this->plugin('translate');
$escape = $this->plugin('escapeHtml');
$hyperlink = $this->plugin('hyperlink');

$this->htmlElement('body')->appendAttribute('class', 'importer jobs undo');
$this->headLink()->appendStylesheet($this->assetUrl('css/import.css', 'Import'));
?>

<?php echo $this->pageTitle($translate('Import'), 1, $translate('Import')); ?>

<h2><?php echo $escape($translate('Available importers')); ?></h2>
<?php echo $hyperlink('Add an importer', $this->url('admin/import/default', ['controller' => 'importer', 'action' => 'add'])); ?>

<?php if (count($importers)): ?>
    <table class="tablesaw" data-tablesaw-mode="stack">
        <thead>
        <tr>
            <th><?php echo $escape($translate('Name')); ?></th>
            <th><?php echo $escape($translate('Reader')); ?></th>
            <th><?php echo $escape($translate('Processor')); ?></th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($importers as $importer): ?>
            <tr>
                <td>
                    <?php echo $hyperlink($importer->getName(), $this->url('admin/import/id', ['controller' => 'importer', 'action' => 'start', 'id' => $importer->getId()])); ?>
                </td>
                <td>
                    <?php $reader = $importer->getReader(); ?>
                    <?php echo $reader->getLabel(); ?>
                    <?php if ($reader instanceof ConfigurableImport): ?>
                        (<?php echo $hyperlink('Configure', $this->url('admin/import/id', ['controller' => 'importer', 'action' => 'configure-reader', 'id' => $importer->getId()])); ?>)
                    <?php endif; ?>
                </td>
                <td>
                    <?php $processor = $importer->getProcessor(); ?>
                    <?php echo $processor->getLabel(); ?>
                    <?php if ($processor instanceof ConfigurableImport): ?>
                        (<?php echo $hyperlink('Configure', $this->url('admin/import/id', ['controller' => 'importer', 'action' => 'configure-processor', 'id' => $importer->getId()])); ?>)
                    <?php endif; ?>
                </td>
                <td>
                <?php echo $hyperlink('Edit', $this->url('admin/import/id', ['controller' => 'importer', 'action' => 'edit', 'id' => $importer->getId()])); ?>
                <?php echo $hyperlink('Delete', $this->url('admin/import/id', ['controller' => 'importer', 'action' => 'delete', 'id' => $importer->getId()])); ?>
                </td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
<?php endif; ?>

<?php if (!empty($imports)): ?>
    <h2><?php echo $escape($translate('Last imports')); ?></h2>
    <?php echo $hyperlink('See all imports', $this->url('admin/import/default', ['controller' => 'import', 'action' => 'index'])); ?>
    <?php echo $this->partial('import/admin/import/import-table', ['imports' => $imports]); ?>
<?php endif; ?>
